import "@radix-ui/themes/styles.css" with { type: "css" };
import { Button, Theme } from "@radix-ui/themes";

function App() {
  return (
    <Theme>
      <Button>Hey 👋</Button>
    </Theme>
  );
}

export default App;
